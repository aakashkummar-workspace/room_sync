class AppConstants {
  static const String appName = 'CasaSync';
  static const String tagline = 'Smart Hostel Automation';
  static const String quote = 'Built for the way roommates actually live.';

  static const List<String> expenseCategories = [
    'General',
    'Groceries',
    'Utilities',
    'Rent',
    'Entertainment',
  ];

  static const List<String> choreStatuses = [
    'pending',
    'completed',
  ];

  static const Duration animDuration = Duration(milliseconds: 300);
  static const Duration toastDuration = Duration(seconds: 4);
}
